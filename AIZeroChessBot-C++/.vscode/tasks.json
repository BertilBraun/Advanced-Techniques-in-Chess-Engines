{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run",
            "type": "shell",
            "command": "cd build && cmake --build . --config Release && cd Release && AIZeroChessBot.exe",
            "problemMatcher": []
        }
    ]
}