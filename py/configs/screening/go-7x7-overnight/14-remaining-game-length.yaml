extends: 00-baseline.yaml
run:
  run_name: go7-overnight-14-remaining-game-length
  tensorboard_run_directory: go7-overnight-14-remaining-game-length
training:
  save_path: py/training_data/screening/go7-overnight/14-remaining-game-length
go:
  objective:
    auxiliary_targets:
      - kind: remaining_game_length
        normalization_scale: 196
        loss_weight: 0.15
      - kind: search_budget
        loss_weight: 0.2
