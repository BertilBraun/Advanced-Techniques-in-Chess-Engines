# Chess teacher-imitation distillation probe (RTX 3060, 2026-08-27)

Can a trained chess network be distilled into a much smaller one by imitating its raw policy and value
heads, and does the smaller network's extra search speed make up for its weaker prior?

**Yes to the first. No to the second.** A 1.33M-parameter student reaches −38.4 Elo against the 6.06M
teacher it was distilled from at equal compute and 25 teacher searches per move. But the gap widens
monotonically with search depth on every model tested, so the small network's advantage exists only where
search barely matters. Data quantity, not parameter count, was the dominant lever.

## Provenance

| | |
| --- | --- |
| Source revision | `775c4c34a9610252866619e45377750fa73271f5` (branch `distillation-probe`, based on `e8bec367`) |
| Node | Vast.ai `50.120.65.61:41841` — 1× RTX 3060 12 GiB, driver 595.84, Ryzen, 56 effective CPUs, 62 GiB RAM, 150 GiB overlay |
| Runtime | torch 2.12.1+cu126, Python 3.12, Release native extension built at `e8bec367` |
| Teacher | `vast-chess-4day-production-v8` generation 322, 14x152, `model_322.pt` sha256 `dd514db199186f6e657593210751b7f6b1dce9b4e172129c9d2944e38b86f3df` |
| Match configuration | `py/configs/validation/distillation-probe-chess-single-gpu.yaml`, file sha256 `8caaca0ff5e4fbd90fcb8d566b53d5ebb7d630166666599751dbc463f8c45f11`, resolved `experiment_configuration_sha256` `c1ac342fbb33e959258814474f9709d33752f3833c99981e590837a8fd910e2d` |
| Openings | `chess-elite-2025-11-balanced-4moves-200-v1`, manifest sha256 `ab15a513135c7aec3e19c0ce3b845e03c2f47a9a73f07a72f5a9ab4842470c1e`, 200 openings → 400 paired games per match |

The teacher is a **weak** reference: v8 is the run with the documented endgame conversion defect. Absolute
Elo differences here are not transferable to a stronger teacher; the lever ranking and the search-depth
behaviour are the transferable results.

Numbers were measured on an RTX 3060 and must not be compared against the RTX 4070 SUPER benchmarks.

## Method

No MCTS anywhere in the labelling path. Positions are generated by batched self-play driven by the
teacher's own policy head at temperature 1.0, opened with 4 uniformly random legal plies and perturbed
with a uniformly random legal move at a configured probability. Each recorded position stores the
teacher's masked-softmax policy over the legal set (top 64 entries, renormalised), the legal action ids,
and the teacher's softmax WDL.

The student is trained with the production `ResolvedTrainingObjective` unchanged — its policy term is
already soft-target cross-entropy and does not know or care that targets came from visit counts. Setting
`root_value_blend` to 0 makes the value term exactly cross-entropy against the teacher's WDL. No
auxiliary heads were distilled in this probe.

`gap above floor` is held-out policy cross-entropy minus the target-entropy floor of the held-out set.
Raw cross-entropy is not comparable across datasets because the floor moves with the generator; the gap
is.

## Datasets

| dataset | positions | games | positions per game | recorded ply interval | perturbation | floor (policy) |
| --- | --- | --- | --- | --- | --- | --- |
| 1M | 1,000,000 | 21,601 | 46 | 4 | 0.05 | 1.9677 |
| 5M | 5,000,000 | ~219,000 | 23 | 8 | 0.10 | 1.9938 |
| 6M (merged) | 6,000,000 | ~240,000 | 25 | — | — | 1.9938 |

Generation rate: 1327 positions/s at interval 4, 441 positions/s at interval 8 (partly contended), 512
and 1024 parallel games respectively.

## Training results

All students use `policy_head: {kind: dense, channels: 4, bottleneck_rank: 16}`, which holds the head
cost at a flat ~43k parameters regardless of backbone width.

| student | params | dataset | steps | gap above floor | train/held-out separation |
| --- | --- | --- | --- | --- | --- |
| 6x64 | 480,647 | 1M | 8,000 | 0.3843 | 0.017 |
| 6x96 | 1,014,255 | 1M | 8,000 | 0.3540 | — |
| 14x64 | 1,041,991 | 1M | 8,000 | 0.3712 | — |
| 8x96 | 1,329,591 | 1M | 8,000 | 0.3407 | — |
| 6x64 | 480,647 | 1M | 32,000 | 0.3108 | **0.077** |
| 6x64 | 480,647 | 6M | 80,000 | 0.2097 | 0.024 |
| 8x96 | 1,329,591 | 6M | 80,000 | **0.1584** | 0.024 |

The 0.077 separation at 32,000 steps on 1M positions is the point the data constraint became binding:
training loss kept falling while held-out went slightly backwards. On 6M positions the same architecture
trained 2.5× longer and still only separated by 0.024.

## Evaluation results

400 paired games per row, teacher = v8 generation 322 (6.06M parameters as loaded for inference).
Elo is the student's difference from the teacher; intervals are 95% from the paired bootstrap over
opening pairs.

### Students trained on 6M positions

| student | Tier A equal-nodes (25/25) | Tier A equal-compute | Tier B equal-nodes (250/250) |
| --- | --- | --- | --- |
| 6x64 (0.48M) | −246.3 [−275.5, −219.9] | −75.0 [−100.0, −49.8] @ 89 searches | −470.4 [−535.8, −415.6] |
| 8x96 (1.33M) | −176.1 [−205.0, −148.2] | **−38.4 [−62.3, −14.8]** @ 58 searches | −257.6 [−293.0, −225.0] |

The 8x96 at equal compute went 87 wins / 182 draws / 131 losses, score 0.445.

### The same 0.48M model across the whole probe

| dataset and schedule | Tier A equal-nodes | Tier A equal-compute |
| --- | --- | --- |
| 1M, 8,000 steps | −404.6 | −324.5 @ 97 searches |
| 1M, 32,000 steps | −320.8 | −200.2 @ 142 searches |
| 6M, 80,000 steps | −246.3 | −75.0 @ 89 searches |

+249 Elo at equal compute, none of it from adding parameters, and the final figure was achieved on a
*smaller* search budget than the middle one.

## What moved the needle

| lever | change | Elo |
| --- | --- | --- |
| Training data | 1M → 6M positions | ~+125 |
| Training schedule | 8k → 32k steps | ~+84 |
| Parameters, data-limited | 0.48M → 1.33M on 1M positions | ~0 |
| Parameters, data-sufficient | 0.48M → 1.33M on 6M positions | ~+37 |
| Search budget | ~3.6× more searches at 25 teacher searches | ~+80 |

Parameters bought nothing while data was binding and started paying once it was not. Any architecture
conclusion drawn under a data constraint is unreliable — the four-model sweep on 1M positions spanned
0.044 nats, less than the 0.075 a schedule change bought on a single model, so its width-versus-depth
ordering was inside the noise of a different effect.

Held-out cross-entropy is a usable cheap proxy for playing strength. Two independent conversions agree at
roughly **1,100–1,400 Elo per nat** of gap: 0.075 nats → +84 Elo (schedule), 0.051 nats → +70 Elo
(6x64 versus 8x96 at equal nodes). At equal nodes the four-model ordering by Elo matched the ordering by
cross-entropy exactly. Tune students on cross-entropy; spend game time only on finalists.

## Search depth widens the gap

The central negative result, measured on four independently trained students:

| student | equal nodes at 25 | equal nodes at 250 |
| --- | --- | --- |
| 6x64 on 6M | −246.3 | −470.4 |
| 8x96 on 6M | −176.1 | −257.6 |

Giving both sides ten times the search consistently makes the student *worse*, not better. MCTS explores
the moves the prior nominates, so a better prior compounds under deeper search rather than being caught
up with. Distillation transfers the function; it does not transfer whatever makes the teacher's prior
compound.

This is why the "small model plus more search wins at equal time" thesis fails. The student's advantage
is real but confined to shallow search.

## Throughput

Measured through the evaluation search path, 200 roots, `parallel_searches: 1`.

| net | params | positions/s | ratio to teacher |
| --- | --- | --- | --- |
| teacher 14x152 | 6,058,677 | ~12,573 | 1.00 |
| 6x64 | 480,647 | ~50,653 | ~4.0 |
| 6x96 | 1,014,255 | ~31,015 | ~2.4 |
| 14x64 | 1,041,991 | ~34,658 | ~2.7 |
| 8x96 | 1,329,591 | ~28,644 | ~2.3 |

A 12.6× parameter reduction buys only ~4× throughput. With `parallel_searches: 1` the inference batch
equals the root count, `worker_utilization` sits at 0.03–0.14, and roughly 3% of wall time is inside the
forward pass — **the search is CPU and launch bound, not network bound**. Better batching would speed the
teacher more than the students and shrink their advantage further.

Root count matters enormously and must match the match's own batching: the teacher measured 4,435
positions/s at 32 roots and ~12,573 at 200. Depth costs launch latency only at small batches — 6x96 beat
14x64 by 1.51× at one root and lost to it by 1.12× at 200.

## Known defects and caveats

1. **All recorded positions have the same side to move.** `distill_build_dataset.py` records when
   `ply % recorded_ply_interval == 0` and `ply` starts at a fixed `random_opening_plies`, so with an even
   interval every recorded ply is even. Chess inputs are canonical (side-to-move perspective), which
   bounds the harm, but for any exchange sequence only one side of it is ever sampled. Fix by sampling
   positions at random rather than at a fixed stride.
2. **Live throughput measurement is too noisy for equal-compute budgets.** Repeated measurements of one
   model spanned 3.87–5.69, moving equal-compute Elo by tens of points. `--pinned-throughput-ratio` now
   exists; use one careful measurement on an idle GPU and hold it fixed across matchups. Contention
   raises the ratio, because the teacher's larger batches suffer more than the student's.
3. **A foreign workload occupied the node** (9.7 GiB, 49% utilisation) during part of the session. Match
   outcomes at fixed node counts are unaffected by contention; only throughput-derived budgets are.
   Timings in this note are contended unless stated.
4. **`save_model_and_optimizer` must not be used for students.** Its generation-0 branch runs
   `calibrate_bootstrap_policy_prior`, which rescales the exported policy head and would mean the
   evaluated student is not the trained student. `distill_train_student.py` writes the checkpoint
   directly instead.
5. **Teacher and student must share a generation number.** `create_evaluation_search` resolves
   generation-scheduled parameters such as `value_discount_per_ply` from the checkpoint's generation and
   the evaluation config does not override them.
6. **Positions above 64 legal moves are truncated** to the top 64 and renormalised. Observed maximum was
   67 legal moves; tail mass beyond rank 64 is negligible but nonzero.
7. The held-out split is the contiguous tail of the same generator, so it measures generalisation to new
   positions from the same distribution, not distribution shift.

## Requirements for distilling a stronger teacher

1. **Sample positions randomly, not at a fixed ply stride.** Collect every ply and keep each with
   probability 1/N. This removes the parity defect above and the periodic structure a fixed stride
   imposes. N in the region of 12–16 gives good within-game decorrelation.
2. **Budget far more positions than feels necessary.** 1M positions was clearly insufficient for a 0.48M
   model; 6M was adequate but the larger student had begun to separate. For a 10M teacher and students in
   the 1–4M range, expect to need tens of millions. AlphaZero-scale supervised work uses hundreds of
   millions, so 10M remains at the low end.
3. **Generation cost scales badly and is the long pole.** At 1/14 sampling with a 10M teacher, expect
   roughly a quarter of this probe's interval-8 rate per GPU. Generation is embarrassingly parallel across
   GPUs; plan for that rather than a single-device run.
4. **Spend the schedule at high learning rate.** In both 80,000-step runs the final quarter moved the gap
   by 0.004 and 0.003 — nearly free of return, worth about 5 Elo. A longer high-rate phase with the
   anneal compressed into the last ~20% should dominate a full-length cosine.
5. **Fix the throughput ratio once, on an idle GPU, at the root count the match will use.**
6. **Report gap above floor, not raw cross-entropy**, and re-measure the floor whenever the generator
   changes.
7. Evaluate at the search depth you actually intend to play at. Shallow-search results flatter the
   student substantially.

## Sharded generation on a multi-GPU node

`distill_build_dataset.py` drives one device. To use a node with several, run one process per device and
merge the shards afterwards. No additional code is needed — `distill_merge_datasets.py` accepts `--input`
repeatedly and validates that every shard shares a teacher and record layout before concatenating.

**Every shard must get a different `--random-seed`.** The seed drives opening selection, move sampling and
perturbation, so eight processes sharing a seed produce eight byte-identical shards and a dataset with an
eighth of its apparent content. The merge tool checks compatibility, not novelty, and will not catch this.

```
cd /workspace/alphazero-engine/py
for device in 0 1 2 3 4 5 6 7; do
  nohup /workspace/alphazero-engine-venv/bin/python -m tools.distill_build_dataset \
    --teacher-run-state <run> --teacher-generation <N> \
    --teacher-layers <L> --teacher-hidden-size <H> \
    --output /workspace/distill/shard-${device}.bin --positions <count_per_shard> \
    --parallel-games 1024 --recorded-ply-interval <N> \
    --random-perturbation-probability 0.10 \
    --random-seed $((20260827 + device)) --device-id ${device} \
    > /workspace/distill/shard-${device}.log 2>&1 &
done
wait
```

Verify the shards actually differ before spending training time on them:

```
sha256sum /workspace/distill/shard-*.bin        # eight distinct digests, or the seeds did not vary
python -m tools.distill_merge_datasets $(printf -- '--input /workspace/distill/shard-%d.bin ' 0 1 2 3 4 5 6 7) \
  --output /workspace/distill/chess-distill-merged.bin
```

The merged manifest keeps the last shard's generator settings, because its rows form the held-out tail.
Order the inputs so the shard you want the held-out split drawn from comes last.

Two node caveats. `setup_remote.sh` **exited 0 on a node where the engine install had failed** (the KataGo
download died with `curl: (56)` and Stockfish was never installed), so check `engines/` explicitly rather
than trusting the exit status — it is needed by `build_chess_opening_manifest.py`, though not by
generation or by checkpoint-versus-checkpoint matches. And confirm the GPUs are otherwise idle
(`nvidia-smi --query-compute-apps=pid,used_memory --format=csv,noheader`); a foreign workload silently
distorted throughput measurements during this probe.

Note `pgrep -f <pattern>` matches the polling shell's own command line and will never report the job gone.
Poll on a sentinel line in the log, or on `nvidia-smi`, instead.

## Reproduction

```
python -m tools.distill_build_dataset --teacher-run-state <run> --teacher-generation <N> \
  --teacher-layers 14 --teacher-hidden-size 152 --output <bin> --positions <count> \
  --parallel-games 1024 --recorded-ply-interval 8 --random-perturbation-probability 0.10 --random-seed <seed>

python -m tools.distill_merge_datasets --input <a.bin> --input <b.bin> --output <merged.bin>

python -m tools.distill_train_student --dataset <merged.bin> --output-run-state <students/name> \
  --layers 8 --hidden-size 96 --policy-bottleneck-rank 16 --batch-size 1024 --steps 80000 \
  --evaluate-every 4000 --warmup-steps 1000 --generation <N>

python -m tools.distill_match --teacher-run-state <run> --teacher-generation <N> \
  --student-run-state <students/name> --student-generation <N> --openings-manifest <json> \
  --mode equal-nodes --searches-per-move 25 --opening-pair-count 200 \
  --experiment-config py/configs/validation/distillation-probe-chess-single-gpu.yaml --output <json>
```

Raw evaluation JSON, the training logs and the merged dataset manifest were fetched off the ephemeral node
to `.codex-diagnostics/chess-distillation-probe-20260827/` (44 files, 14 MiB). The teacher checkpoint is
retained separately at `.codex-diagnostics/v8-teacher-g322/`, sha256-verified against its manifest.
